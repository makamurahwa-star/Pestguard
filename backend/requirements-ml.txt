# Optional: pest detection model dependencies.
# Install only when you have a trained YOLO .pt model to plug in.
#
# Works on Python 3.10–3.12 (you have venv312, so you're set).
# On Python 3.13+ wait for newer torch releases.
#
# Install with:
#   pip install -r requirements-ml.txt
#
ultralytics>=8.3
torch>=2.1
torchvision>=0.16
