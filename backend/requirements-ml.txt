# Optional ML dependencies — CPU-only Torch for free hosting tiers (~150 MB
# instead of ~2.5 GB of unused CUDA libraries).
#
# The --extra-index-url line points pip at PyTorch's CPU-only wheels.
# torch and torchvision must be pinned to versions that have CPU wheels
# available on that index.

--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.4.1+cpu
torchvision==0.19.1+cpu
ultralytics>=8.3
